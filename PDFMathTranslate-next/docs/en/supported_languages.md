> [!NOTE]
> `PDFTranslate` currently follows the same language coverage baseline as the bundled `BabelDOC` engine. If you need to verify a specific language pair, test it directly in the current repository build and document any confirmed differences here.
